<div
  <% unless back %>name="<%= card.id %>"<% end %>
  class="card
         <%= back ? '_back' : ('_' + card.id.downcase) %>
         <%= !back && card.chosen ? 'chosen' : '' %>
         <%= selectable ? 'selectable' : '' %>"
  style="top: <%= y %>px; left: <%= x %>px">
    &nbsp;
</div>
